[project]
name = "matchcast"
version = "0.1.0"
description = "Backend-first probabilistic football prediction engine for international matches"
readme = "README.md"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
